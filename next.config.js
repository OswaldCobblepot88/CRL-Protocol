/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Указываем Next.js, что директория со страницами находится в папке frontend
  distDir: "../.next",
};

module.exports = nextConfig;